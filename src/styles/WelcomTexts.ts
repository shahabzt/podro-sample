import styled from "styled-components";

export const WelcomeTexts = styled.div({
    display: "flex",
    flexDirection: "column",
    rowGap: "8px",
    margin: "32px 0",
    alignItems: "center",
  });